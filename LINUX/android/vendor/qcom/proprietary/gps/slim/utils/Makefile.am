# Makefile.am - Automake script for gps garden shippable test app
#

ACLOCAL_AMFLAGS = -I m4

SUBDIRS = src

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = slim-utils.pc
EXTRA_DIST = $(pkgconfig_DATA)
