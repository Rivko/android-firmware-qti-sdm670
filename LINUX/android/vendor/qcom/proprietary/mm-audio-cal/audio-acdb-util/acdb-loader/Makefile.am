# Makefile.am - Automake script for acdbloader

ACLOCAL_AMFLAGS = -I m4

dirs := src

SUBDIRS = $(dirs)

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = acdbloader.pc
EXTRA_DIST = $(pkgconfig_DATA)
